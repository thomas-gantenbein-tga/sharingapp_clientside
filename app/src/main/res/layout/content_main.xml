<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="18dp"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".activities.MainActivity"
    tools:showIn="@layout/activity_main">

    <Button
        android:id="@+id/showAllArticles"
        style="@style/ButtonStyle"
        android:onClick="showArticleList"
        android:text="Alle Artikel anzeigen"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.45"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/createNewArticle"
        style="@style/ButtonStyle"
        android:onClick="createNewArticle"
        android:text="Neuen Artikel anlegen"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.45"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/showAllArticles" />

    <Button
        android:id="@+id/findArticle"
        style="@style/ButtonStyle"
        android:onClick="findArticle"
        android:text="Artikel suchen"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.45"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/createNewArticle" />

    <Button
        android:id="@+id/deleteArticle"
        style="@style/ButtonStyle"
        android:onClick="editOwnArticles"
        android:text="Meine Artikel"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.45"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/findArticle" />

</android.support.constraint.ConstraintLayout>